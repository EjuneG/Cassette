name: Release

on:
  push:
    tags:
      - v*
  workflow_dispatch:

jobs:
  release:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [macos-latest, windows-latest, ubuntu-22.04]

    steps:
      - name: Check out Git repository
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'yarn'

      - name: Install RPM & Pacman (on Ubuntu)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update &&
          sudo apt-get install --no-install-recommends -y rpm libarchive-tools libopenjp2-tools

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Build renderer (Vite)
        # Must be the Electron renderer build, not the web one: without
        # IS_ELECTRON the bundle gets the history router and every
        # `process.env.IS_ELECTRON` guard goes false — no titlebar, no tray
        # sync, no UNM, no mini player. Set via env rather than the
        # `build:electron` script, whose `IS_ELECTRON=true` prefix is not
        # valid on the Windows runner's shell.
        run: yarn build
        env:
          IS_ELECTRON: 'true'

      - name: Build Electron main process
        run: yarn electron:build-main

      - name: Build Electron app
        run: npx electron-builder --config electron-builder.yml --publish always
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/upload-artifact@v4
        with:
          name: YesPlayMusic-${{ matrix.os }}
          path: |
            dist_electron/*.dmg
            dist_electron/*.exe
            dist_electron/*.AppImage
          if-no-files-found: ignore
